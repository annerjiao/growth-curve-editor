# Optional: URL of the Cloudflare Worker for Claude milestone parsing.
# Local dev with worker: VITE_MILESTONE_PARSE_URL=http://localhost:8787
# Production: set in GitHub Actions repo variables or .env.production.local

VITE_MILESTONE_PARSE_URL=
